pkgname=ramen
pkgver=1
pkgrel=1
pkgdesc="Ramen, trin for trin!"
arch=("any")
source=("ramen.png")
sha256sums=("SKIP")

package() {
	mkdir -p $pkgdir/usr/share
	cp $srcdir/ramen.png $pkgdir/usr/share/
}
